<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>New Project</title>
  <link rel="stylesheet" href="/stylesheets/style.css">
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      background: #f8f9fa;
      color: #333;
    }
    .container {
      max-width: 900px;
      margin: 2rem auto;
      padding: 0 1rem;
    }
    h1 {
      color: #2c3e50;
      margin-bottom: 1.5rem;
    }
    label {
      display: block;
      margin: 1.2rem 0 0.4rem;
      font-weight: bold;
      color: #495057;
    }
    input, textarea {
      width: 100%;
      padding: 10px;
      border: 1px solid #ced4da;
      border-radius: 6px;
      box-sizing: border-box;
      font-size: 1rem;
    }
    textarea {
      height: 140px;
      resize: vertical;
    }
    .member-checkbox {
      margin: 0.6rem 0;
      font-size: 1rem;
    }
    button {
      padding: 14px 32px;
      background: #28a745;
      color: white;
      border: none;
      border-radius: 6px;
      font-size: 1.1rem;
      cursor: pointer;
      margin-top: 1.5rem;
      transition: background 0.2s;
    }
    button:hover {
      background: #218838;
    }
    a.cancel {
      padding: 14px 32px;
      background: #6c757d;
      color: white;
      text-decoration: none;
      border-radius: 6px;
      margin-left: 1rem;
      font-size: 1.1rem;
    }
    .message {
      padding: 1rem;
      margin-bottom: 1.5rem;
      border-radius: 6px;
    }
    .success { background: #d4edda; color: #155724; }
    .error   { background: #f8d7da; color: #721c24; }
  </style>
</head>
<body>

  <%- include('../partials/header') %>

  <div class="container">
    <h1>Create New Project</h1>

    <% if (success_msg) { %>
      <div class="message success"><%= success_msg %></div>
    <% } %>
    <% if (error_msg) { %>
      <div class="message error"><%= error_msg %></div>
    <% } %>

    <form action="/projects" method="POST">

      <label for="name">Project Name</label>
      <input type="text" id="name" name="project[name]" required placeholder="e.g. Website Redesign 2026">

      <label for="description">Description</label>
      <textarea id="description" name="project[description]" required placeholder="Describe the project goals, scope, requirements..."></textarea>

      <label for="price">Price (€)</label>
      <input type="number" id="price" name="project[price]" step="0.01" min="0" required placeholder="e.g. 4500.00">

      <label for="completedJobs">Completed Jobs / Progress So Far</label>
      <textarea id="completedJobs" name="project[completedJobs]" placeholder="Describe what has been done so far (e.g. 'Planning completed, design approved, first sprint in progress')"></textarea>

      <label for="startDate">Start Date</label>
      <input type="date" id="startDate" name="project[startDate]" required>

      <label for="endDate">End Date</label>
      <input type="date" id="endDate" name="project[endDate]" required>

      <h2>Assign Team Members</h2>
      <% if (members && members.length > 0) { %>
        <% members.forEach(user => { %>
          <label class="member-checkbox">
            <input type="checkbox" name="members" value="<%= user._id %>">
            <%= user.username %>
          </label>
        <% }) %>
      <% } else { %>
        <p style="color: #666; font-style: italic;">
          No users registered yet.
        </p>
      <% } %>

      <div style="margin-top: 2rem;">
        <button type="submit">Create Project</button>
        <a href="/projects" class="cancel">Cancel</a>
      </div>
    </form>
  </div>

</body>
</html>